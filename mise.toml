# mise watch test
[tasks.test]
run = "vusted tests"
sources = ["**/*.lua"]
outputs = []

[tasks.generate-doc]
run = "nvim --headless --noplugin -u ./scripts/minimal_init.lua -c 'luafile ./scripts/generate_doc.lua' -c 'qa!'"
